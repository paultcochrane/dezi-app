#!/usr/bin/env perl
use strict;
use warnings;
use FindBin;
use lib "$FindBin::Bin/../lib";
use Dezi::CLI;

our $VERSION = $Dezi::CLI::VERSION;

my $app = Dezi::CLI->new_with_options();
$app->run();
exit(0);

__END__

